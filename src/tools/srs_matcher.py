"""SRS-to-code matching helper using chunking and embeddings."""

from __future__ import annotations


def match_requirements(repo_path: str, srs_path: str | None = None):
    """Placeholder requirement matcher."""
    return {
        "repo_path": repo_path,
        "srs_path": srs_path,
        "matches": [],
        "coverage": 0.0,
    }
